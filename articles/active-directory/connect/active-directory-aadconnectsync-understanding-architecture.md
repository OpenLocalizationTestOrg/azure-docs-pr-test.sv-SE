---
title: "Azure AD Connect-synkronisering: Förstå arkitekturen | Microsoft Docs"
description: "Det här avsnittet beskrivs arkitekturen i Azure AD Connect-synkronisering och beskrivs de termer som används."
services: active-directory
documentationcenter: 
author: andkjell
manager: femila
editor: 
ms.assetid: 465bcbe9-3bdd-4769-a8ca-f8905abf426d
ms.service: active-directory
ms.workload: identity
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 07/13/2017
ms.author: billmath
ms.openlocfilehash: 51082ad453d53f56f30f814b78578801c00f4827
ms.sourcegitcommit: 02e69c4a9d17645633357fe3d46677c2ff22c85a
ms.translationtype: MT
ms.contentlocale: sv-SE
ms.lasthandoff: 08/03/2017
---
# <a name="azure-ad-connect-sync-understanding-the-architecture"></a><span data-ttu-id="f359f-103">Azure AD Connect-synkronisering: Förstå arkitekturen</span><span class="sxs-lookup"><span data-stu-id="f359f-103">Azure AD Connect sync: Understanding the architecture</span></span>
<span data-ttu-id="f359f-104">Det här avsnittet beskriver de grundläggande arkitekturen för Azure AD Connect-synkronisering.</span><span class="sxs-lookup"><span data-stu-id="f359f-104">This topic covers the basic architecture for Azure AD Connect sync.</span></span> <span data-ttu-id="f359f-105">Det är många aspekter liknar föregående MIIS 2003 ILM 2007 och FIM 2010.</span><span class="sxs-lookup"><span data-stu-id="f359f-105">In many aspects, it is similar to its predecessors MIIS 2003, ILM 2007, and FIM 2010.</span></span> <span data-ttu-id="f359f-106">Azure AD Connect-synkronisering är utvecklingen av dessa tekniker.</span><span class="sxs-lookup"><span data-stu-id="f359f-106">Azure AD Connect sync is the evolution of these technologies.</span></span> <span data-ttu-id="f359f-107">Om du är bekant med någon av dessa tidigare tekniker kommer innehållet i det här avsnittet att bekanta dig även.</span><span class="sxs-lookup"><span data-stu-id="f359f-107">If you are familiar with any of these earlier technologies, the content of this topic will be familiar to you as well.</span></span> <span data-ttu-id="f359f-108">Om du är nybörjare på synkronisering, är det här avsnittet för dig.</span><span class="sxs-lookup"><span data-stu-id="f359f-108">If you are new to synchronization, then this topic is for you.</span></span> <span data-ttu-id="f359f-109">Det är emellertid inte ett krav att känna till det här avsnittet ska lyckas i gör anpassningar i Azure AD Connect-synkronisering (kallas Synkroniseringsmotorn i det här avsnittet).</span><span class="sxs-lookup"><span data-stu-id="f359f-109">It is however not a requirement to know the details of this topic to be successful in making customizations to Azure AD Connect sync (called sync engine in this topic).</span></span>

## <a name="architecture"></a><span data-ttu-id="f359f-110">Arkitektur</span><span class="sxs-lookup"><span data-stu-id="f359f-110">Architecture</span></span>
<span data-ttu-id="f359f-111">Synkroniseringsmotorn skapar en integrerad vy av objekt som lagras i flera anslutna datakällor och hanterar identitetsinformation i dessa datakällor.</span><span class="sxs-lookup"><span data-stu-id="f359f-111">The sync engine creates an integrated view of objects that are stored in multiple connected data sources and manages identity information in those data sources.</span></span> <span data-ttu-id="f359f-112">Den här integrerade vyn bestäms av identitetsinformationen hämtades från anslutna datakällor och en uppsättning regler som bestämmer hur att bearbeta den här informationen.</span><span class="sxs-lookup"><span data-stu-id="f359f-112">This integrated view is determined by the identity information retrieved from connected data sources and a set of rules that determine how to process this information.</span></span>

### <a name="connected-data-sources-and-connectors"></a><span data-ttu-id="f359f-113">Anslutna datakällor och kopplingar</span><span class="sxs-lookup"><span data-stu-id="f359f-113">Connected Data Sources and Connectors</span></span>
<span data-ttu-id="f359f-114">Synkroniseringsmotorn bearbetar identitetsinformation från olika databaser, till exempel Active Directory eller en SQL Server-databas.</span><span class="sxs-lookup"><span data-stu-id="f359f-114">The sync engine processes identity information from different data repositories, such as Active Directory or a SQL Server database.</span></span> <span data-ttu-id="f359f-115">Alla data databaser som organiserar data i en databas som format och som ger standard dataåtkomst metoder är en potentiell datakälla kandidat för Synkroniseringsmotorn.</span><span class="sxs-lookup"><span data-stu-id="f359f-115">Every data repository that organizes its data in a database-like format and that provides standard data-access methods is a potential data source candidate for the sync engine.</span></span> <span data-ttu-id="f359f-116">Data-databaser som synkroniseras av Synkroniseringsmotorn kallas **anslutna datakällor** eller **anslutna kataloger** (CD).</span><span class="sxs-lookup"><span data-stu-id="f359f-116">The data repositories that are synchronized by sync engine are called **connected data sources** or **connected directories** (CD).</span></span>

<span data-ttu-id="f359f-117">Synkroniseringsmotorn kapslar in interaktion med en ansluten datakälla i en modul som kallas en **Connector**.</span><span class="sxs-lookup"><span data-stu-id="f359f-117">The sync engine encapsulates interaction with a connected data source within a module called a **Connector**.</span></span> <span data-ttu-id="f359f-118">Varje typ av anslutna datakällan har en viss koppling.</span><span class="sxs-lookup"><span data-stu-id="f359f-118">Each type of connected data source has a specific Connector.</span></span> <span data-ttu-id="f359f-119">Kopplingen omvandlar en obligatorisk åtgärd i det format som kan användas med den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-119">The Connector translates a required operation into the format that the connected data source understands.</span></span>

<span data-ttu-id="f359f-120">Kopplingar gör API-anrop för att utbyta identitetsinformation (både läsning och skrivning) med en ansluten datakälla.</span><span class="sxs-lookup"><span data-stu-id="f359f-120">Connectors make API calls to exchange identity information (both read and write) with a connected data source.</span></span> <span data-ttu-id="f359f-121">Det är också möjligt att lägga till en anpassad koppling med hjälp av extensible connectivity framework.</span><span class="sxs-lookup"><span data-stu-id="f359f-121">It is also possible to add a custom Connector using the extensible connectivity framework.</span></span> <span data-ttu-id="f359f-122">Följande bild visar hur en koppling ansluter en ansluten datakälla till Synkroniseringsmotorn.</span><span class="sxs-lookup"><span data-stu-id="f359f-122">The following illustration shows how a Connector connects a connected data source to the sync engine.</span></span>

![Ark1](./media/active-directory-aadconnectsync-understanding-architecture/arch1.png)

<span data-ttu-id="f359f-124">Data kan flöda i båda riktningarna, men det går inte att flöda i båda riktningarna samtidigt.</span><span class="sxs-lookup"><span data-stu-id="f359f-124">Data can flow in either direction, but it cannot flow in both directions simultaneously.</span></span> <span data-ttu-id="f359f-125">Med andra ord en anslutning kan konfigureras så att data kan flöda från den anslutna datakällan till Synkroniseringsmotorn eller Synkroniseringsmotorn till den anslutna datakällan, men endast en av dessa åtgärder kan ske när som helst för ett objekt och attribut.</span><span class="sxs-lookup"><span data-stu-id="f359f-125">In other words, a Connector can be configured to allow data to flow from the connected data source to sync engine or from sync engine to the connected data source, but only one of those operations can occur at any one time for one object and attribute.</span></span> <span data-ttu-id="f359f-126">Riktning kan vara olika för olika objekt och för olika attribut.</span><span class="sxs-lookup"><span data-stu-id="f359f-126">The direction can be different for different objects and for different attributes.</span></span>

<span data-ttu-id="f359f-127">Om du vill konfigurera en koppling, kan du ange de objekttyper som du vill synkronisera.</span><span class="sxs-lookup"><span data-stu-id="f359f-127">To configure a Connector, you specify the object types that you want to synchronize.</span></span> <span data-ttu-id="f359f-128">Ange typerna av objekt definierar vilka objekt som ingår i synkroniseringsprocessen.</span><span class="sxs-lookup"><span data-stu-id="f359f-128">Specifying the object types defines the scope of objects that are included in the synchronization process.</span></span> <span data-ttu-id="f359f-129">Nästa steg är att välja de attribut du vill synkronisera, som kallas en attributet inkluderingslistan.</span><span class="sxs-lookup"><span data-stu-id="f359f-129">The next step is to select the attributes to synchronize, which is known as an attribute inclusion list.</span></span> <span data-ttu-id="f359f-130">De här inställningarna kan ändras varje gång som svar på ändringar till affärsregler.</span><span class="sxs-lookup"><span data-stu-id="f359f-130">These settings can be changed any time in response to changes to your business rules.</span></span> <span data-ttu-id="f359f-131">Dessa inställningar konfigureras för dig när du använder Azure AD Connect-installationsguiden.</span><span class="sxs-lookup"><span data-stu-id="f359f-131">When you use the Azure AD Connect installation wizard, these settings are configured for you.</span></span>

<span data-ttu-id="f359f-132">Om du vill exportera objekt till en ansluten datakälla måste inkluderingslistan attributet innehålla minst de minsta attribut som krävs för att skapa en viss objekttyp i en ansluten datakälla.</span><span class="sxs-lookup"><span data-stu-id="f359f-132">To export objects to a connected data source, the attribute inclusion list must include at least the minimum attributes required to create a specific object type in a connected data source.</span></span> <span data-ttu-id="f359f-133">Till exempel den **sAMAccountName** attributet måste inkluderas i inkluderingslistan attribut för att exportera ett användarobjekt till Active Directory eftersom alla användarobjekt i Active Directory måste ha en **sAMAccountName** attribut.</span><span class="sxs-lookup"><span data-stu-id="f359f-133">For example, the **sAMAccountName** attribute must be included in the attribute inclusion list to export a user object to Active Directory because all user objects in Active Directory must have a **sAMAccountName** attribute defined.</span></span> <span data-ttu-id="f359f-134">Installationsguiden har igen och den här konfigurationen åt dig.</span><span class="sxs-lookup"><span data-stu-id="f359f-134">Again, the installation wizard does this configuration for you.</span></span>

<span data-ttu-id="f359f-135">Om den anslutna datakällan använder strukturella komponenter, till exempel partitioner eller behållare för att ordna objekt, kan du begränsa de områden i den anslutna datakällan som används för en viss lösning.</span><span class="sxs-lookup"><span data-stu-id="f359f-135">If the connected data source uses structural components, such as partitions or containers to organize objects, you can limit the areas in the connected data source that are used for a given solution.</span></span>

### <a name="internal-structure-of-the-sync-engine-namespace"></a><span data-ttu-id="f359f-136">Intern struktur för namnområdet som motorn synkronisering</span><span class="sxs-lookup"><span data-stu-id="f359f-136">Internal structure of the sync engine namespace</span></span>
<span data-ttu-id="f359f-137">Synkronisering av hela motorn namnområde består av två namnrymder som lagrar identitetsinformation.</span><span class="sxs-lookup"><span data-stu-id="f359f-137">The entire sync engine namespace consists of two namespaces that store the identity information.</span></span> <span data-ttu-id="f359f-138">De två namnområden är:</span><span class="sxs-lookup"><span data-stu-id="f359f-138">The two namespaces are:</span></span>

* <span data-ttu-id="f359f-139">Anslutningsplatsen (CS)</span><span class="sxs-lookup"><span data-stu-id="f359f-139">The connector space (CS)</span></span>
* <span data-ttu-id="f359f-140">Metaversum (MV)</span><span class="sxs-lookup"><span data-stu-id="f359f-140">The metaverse (MV)</span></span>

<span data-ttu-id="f359f-141">Den **anslutarplats** är ett mellanlagringsområde som innehåller framställningar av avsedda objekt från en ansluten datakälla och attribut som anges i attributet inkluderingslistan.</span><span class="sxs-lookup"><span data-stu-id="f359f-141">The **connector space** is a staging area that contains representations of the designated objects from a connected data source and the attributes specified in the attribute inclusion list.</span></span> <span data-ttu-id="f359f-142">Synkroniseringsmotorn använder anslutningsplatsen att avgöra vad som har ändrats i den anslutna datakällan och att mellanlagra inkommande ändringar.</span><span class="sxs-lookup"><span data-stu-id="f359f-142">The sync engine uses the connector space to determine what has changed in the connected data source and to stage incoming changes.</span></span> <span data-ttu-id="f359f-143">Synkroniseringsmotorn använder också anslutningsplatsen för att mellanlagra utgående ändringar för export till den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-143">The sync engine also uses the connector space to stage outgoing changes for export to the connected data source.</span></span> <span data-ttu-id="f359f-144">Synkroniseringsmotorn har distinkta anslutningsplatsen som ett mellanlagringsområde för varje koppling.</span><span class="sxs-lookup"><span data-stu-id="f359f-144">The sync engine maintains a distinct connector space as a staging area for each Connector.</span></span>

<span data-ttu-id="f359f-145">Med hjälp av ett mellanlagringsområde Synkroniseringsmotorn förblir oberoende av anslutna datakällor och påverkas inte av deras tillgänglighet och tillgänglighet.</span><span class="sxs-lookup"><span data-stu-id="f359f-145">By using a staging area, the sync engine remains independent of the connected data sources and is not affected by their availability and accessibility.</span></span> <span data-ttu-id="f359f-146">Du kan därför bearbeta identitetsinformation när som helst med hjälp av data i mellanlagringsområdet.</span><span class="sxs-lookup"><span data-stu-id="f359f-146">As a result, you can process identity information at any time by using the data in the staging area.</span></span> <span data-ttu-id="f359f-147">Synkroniseringsmotorn kan begära endast ändringar som görs i den anslutna datakällan sedan den senaste kommunikationssessionen avslutas eller push ut bara ändringarna i identitetsinformation som den anslutna datakällan inte har tagits emot ännu, vilket minskar nätverkets trafik mellan Synkroniseringsmotorn och den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-147">The sync engine can request only the changes made inside the connected data source since the last communication session terminated or push out only the changes to identity information that the connected data source has not yet received, which reduces the network traffic between the sync engine and the connected data source.</span></span>

<span data-ttu-id="f359f-148">Dessutom lagras Synkroniseringsmotorn statusinformation om alla objekt som den skapar etapper i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-148">In addition, sync engine stores status information about all objects that it stages in the connector space.</span></span> <span data-ttu-id="f359f-149">När nya data tas emot, utvärderar Synkroniseringsmotorn alltid om data redan har synkroniserats.</span><span class="sxs-lookup"><span data-stu-id="f359f-149">When new data is received, sync engine always evaluates whether the data has already been synchronized.</span></span>

<span data-ttu-id="f359f-150">Den **metaversum** är en lagringsplats som innehåller aggregerade ID-information från flera anslutna datakällor, vilket ger en enda global vy över alla kombinerade objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-150">The **metaverse** is a storage area that contains the aggregated identity information from multiple connected data sources, providing a single global, integrated view of all combined objects.</span></span> <span data-ttu-id="f359f-151">Metaversum-objekt skapas baserat på ID-information som hämtas från anslutna datakällor och en uppsättning regler som kan du anpassa synkroniseringsprocessen.</span><span class="sxs-lookup"><span data-stu-id="f359f-151">Metaverse objects are created based on the identity information that is retrieved from the connected data sources and a set of rules that allow you to customize the synchronization process.</span></span>

<span data-ttu-id="f359f-152">Följande bild visar connector utrymme namnområde och metaversum-namnområde inom Synkroniseringsmotorn.</span><span class="sxs-lookup"><span data-stu-id="f359f-152">The following illustration shows the connector space namespace and the metaverse namespace within the sync engine.</span></span>

![Arch2](./media/active-directory-aadconnectsync-understanding-architecture/arch2.png)

## <a name="sync-engine-identity-objects"></a><span data-ttu-id="f359f-154">Synkronisera motorn identitetsobjekt</span><span class="sxs-lookup"><span data-stu-id="f359f-154">Sync engine identity objects</span></span>
<span data-ttu-id="f359f-155">Objekt i Synkroniseringsmotorn framställningar av antingen objekt i den anslutna datakällan eller integrerad vyn som synkroniserar motorn har av dessa objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-155">The objects in the sync engine are representations of either objects in the connected data source or the integrated view that sync engine has of those objects.</span></span> <span data-ttu-id="f359f-156">Varje synkronisering motorobjekt måste ha en globalt unik identifierare (GUID).</span><span class="sxs-lookup"><span data-stu-id="f359f-156">Every sync engine object must have a globally unique identifier (GUID).</span></span> <span data-ttu-id="f359f-157">GUID ger dataintegritet och snabb förhållanden mellan objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-157">GUIDs provide data integrity and express relationships between objects.</span></span>

### <a name="connector-space-objects"></a><span data-ttu-id="f359f-158">Kopplingen utrymme objekt</span><span class="sxs-lookup"><span data-stu-id="f359f-158">Connector space objects</span></span>
<span data-ttu-id="f359f-159">När Synkroniseringsmotorn kommunicerar med en ansluten datakälla, läser identitetsinformation i den anslutna datakällan och använder informationen för att skapa en representation av objektet identitet i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-159">When sync engine communicates with a connected data source, it reads the identity information in the connected data source and uses that information to create a representation of the identity object in the connector space.</span></span> <span data-ttu-id="f359f-160">Du kan inte skapa eller ta bort de här objekten individuellt.</span><span class="sxs-lookup"><span data-stu-id="f359f-160">You cannot create or delete these objects individually.</span></span> <span data-ttu-id="f359f-161">Du kan manuellt ta bort alla objekt i en anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-161">However, you can manually delete all objects in a connector space.</span></span>

<span data-ttu-id="f359f-162">Alla objekt i anslutningsplatsen har två attribut:</span><span class="sxs-lookup"><span data-stu-id="f359f-162">All objects in the connector space have two attributes:</span></span>

* <span data-ttu-id="f359f-163">En globalt unik identifierare (GUID)</span><span class="sxs-lookup"><span data-stu-id="f359f-163">A globally unique identifier (GUID)</span></span>
* <span data-ttu-id="f359f-164">Ett unikt namn (DN)</span><span class="sxs-lookup"><span data-stu-id="f359f-164">A distinguished name (also known as DN)</span></span>

<span data-ttu-id="f359f-165">Om den anslutna datakällan tilldelar ett unikt attribut till objektet, kan objekt i anslutningsplatsen också att ha fästpunktsattribut.</span><span class="sxs-lookup"><span data-stu-id="f359f-165">If the connected data source assigns a unique attribute to the object, then objects in the connector space can also have an anchor attribute.</span></span> <span data-ttu-id="f359f-166">Fästpunktsattributet identifierar ett objekt i den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-166">The anchor attribute uniquely identifies an object in the connected data source.</span></span> <span data-ttu-id="f359f-167">Synkroniseringsmotorn använder ankaret för att leta upp motsvarande representation av det här objektet i den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-167">The sync engine uses the anchor to locate the corresponding representation of this object in the connected data source.</span></span> <span data-ttu-id="f359f-168">Synkroniseringsmotorn förutsätter att ankarpunkten för ett objekt aldrig ändras under livslängden för objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-168">Sync engine assumes that the anchor of an object never changes over the lifetime of the object.</span></span>

<span data-ttu-id="f359f-169">Många av kopplingarna använder en känd Unik identifierare för att generera ett ankare automatiskt för varje objekt när den har importerats.</span><span class="sxs-lookup"><span data-stu-id="f359f-169">Many of the Connectors use a known unique identifier to generate an anchor automatically for each object when it is imported.</span></span> <span data-ttu-id="f359f-170">Till exempel Active Directory-kopplingen använder den **objectGUID** attribut för en fästpunkt.</span><span class="sxs-lookup"><span data-stu-id="f359f-170">For example, the Active Directory Connector uses the **objectGUID** attribute for an anchor.</span></span> <span data-ttu-id="f359f-171">Du kan ange fästpunkt generation som en del av kopplingens konfiguration av anslutna datakällor som inte tillhandahåller ett tydligt definierade unika identifierare.</span><span class="sxs-lookup"><span data-stu-id="f359f-171">For connected data sources that do not provide a clearly defined unique identifier, you can specify anchor generation as part of the Connector configuration.</span></span>

<span data-ttu-id="f359f-172">Då fallet ankaret skapas från en eller flera attribut för ett objekt som unikt skriver, varken vilka ändringar och som unikt identifierar objektet i anslutningsplatsen (till exempel en anställd tal eller ett användar-ID).</span><span class="sxs-lookup"><span data-stu-id="f359f-172">In that case, the anchor is built from one or more unique attributes of an object type, neither of which changes, and that uniquely identifies the object in the connector space (for example, an employee number or a user ID).</span></span>

<span data-ttu-id="f359f-173">Ett utrymme connector-objekt kan vara något av följande:</span><span class="sxs-lookup"><span data-stu-id="f359f-173">A connector space object can be one of the following:</span></span>

* <span data-ttu-id="f359f-174">Ett fristående objekt</span><span class="sxs-lookup"><span data-stu-id="f359f-174">A staging object</span></span>
* <span data-ttu-id="f359f-175">En platshållare</span><span class="sxs-lookup"><span data-stu-id="f359f-175">A placeholder</span></span>

### <a name="staging-objects"></a><span data-ttu-id="f359f-176">Mellanlagring objekt</span><span class="sxs-lookup"><span data-stu-id="f359f-176">Staging Objects</span></span>
<span data-ttu-id="f359f-177">Ett fristående objekt representerar en instans av de avsedda objekttyperna från den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-177">A staging object represents an instance of the designated object types from the connected data source.</span></span> <span data-ttu-id="f359f-178">Förutom GUID och det unika namnet har alltid ett värde som anger typ av objekt i ett fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-178">In addition to the GUID and the distinguished name, a staging object always has a value that indicates the object type.</span></span>

<span data-ttu-id="f359f-179">Mellanlagring objekt som har importerats alltid har ett värde för fästpunktsattributet.</span><span class="sxs-lookup"><span data-stu-id="f359f-179">Staging objects that have been imported always have a value for the anchor attribute.</span></span> <span data-ttu-id="f359f-180">Mellanlagring objekt som nyligen har etablerats med Synkroniseringsmotorn och håller på att skapas i den anslutna datakällan har inte ett värde för fästpunktsattributet.</span><span class="sxs-lookup"><span data-stu-id="f359f-180">Staging objects that have been newly provisioned by sync engine and are in the process of being created in the connected data source do not have a value for the anchor attribute.</span></span>

<span data-ttu-id="f359f-181">Objekt för Förproduktion utför också aktuella värden för företag-attribut och teknisk information som krävs av Synkroniseringsmotorn för att genomföra synkroniseringen.</span><span class="sxs-lookup"><span data-stu-id="f359f-181">Staging objects also carry current values of business attributes, and operational information needed by sync engine to perform the synchronization process.</span></span> <span data-ttu-id="f359f-182">Funktionsinformation innehåller flaggor som anger vilken typ av uppdateringar som mellanlagras på fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-182">Operational information includes flags that indicate the type of updates that are staged on the staging object.</span></span> <span data-ttu-id="f359f-183">Om ett fristående objekt har fått ny ID-information från den anslutna datakällan som ännu inte bearbetats, objektet har flaggats som **väntande import**.</span><span class="sxs-lookup"><span data-stu-id="f359f-183">If a staging object has received new identity information from the connected data source that has not yet been processed, the object is flagged as **pending import**.</span></span> <span data-ttu-id="f359f-184">Om ett fristående objekt har nya identitetsinformation som ännu inte har exporterats till den anslutna datakällan, den är markerad som **väntande export**.</span><span class="sxs-lookup"><span data-stu-id="f359f-184">If a staging object has new identity information that has not yet been exported to the connected data source, it is flagged as **pending export**.</span></span>

<span data-ttu-id="f359f-185">Ett fristående objekt kan vara en importera eller exportera objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-185">A staging object can be an import object or an export object.</span></span> <span data-ttu-id="f359f-186">Synkroniseringsmotorn skapar ett importobjekt med hjälp av Objektinformation från den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-186">The sync engine creates an import object by using object information received from the connected data source.</span></span> <span data-ttu-id="f359f-187">När Synkroniseringsmotorn tar emot information om finns ett nytt objekt som matchar ett av de typer av objekt som valts i kopplingen, skapar en importobjekt i anslutningsplatsen som en representation av objektet i den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-187">When sync engine receives information about the existence of a new object that matches one of the object types selected in the Connector, it creates an import object in the connector space as a representation of the object in the connected data source.</span></span>

<span data-ttu-id="f359f-188">Följande bild visar ett importobjekt som representerar ett objekt i den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-188">The following illustration shows an import object that represents an object in the connected data source.</span></span>

![Arch3](./media/active-directory-aadconnectsync-understanding-architecture/arch3.png)

<span data-ttu-id="f359f-190">Synkroniseringsmotorn skapar en export-objekt med hjälp av information om objekt i metaversumet.</span><span class="sxs-lookup"><span data-stu-id="f359f-190">The sync engine creates an export object by using object information in the metaverse.</span></span> <span data-ttu-id="f359f-191">Exportera objekt exporteras till den anslutna datakällan under nästa kommunikation.</span><span class="sxs-lookup"><span data-stu-id="f359f-191">Export objects are exported to the connected data source during the next communication session.</span></span> <span data-ttu-id="f359f-192">Ur ett Synkroniseringsmotorn finns exportera objekt inte i den anslutna datakällan ännu.</span><span class="sxs-lookup"><span data-stu-id="f359f-192">From the perspective of the sync engine, export objects do not exist in the connected data source yet.</span></span> <span data-ttu-id="f359f-193">Därför är fästpunktsattributet för en export-objektet inte tillgänglig.</span><span class="sxs-lookup"><span data-stu-id="f359f-193">Therefore, the anchor attribute for an export object is not available.</span></span> <span data-ttu-id="f359f-194">När den tar emot objektet från Synkroniseringsmotorn skapar ett unikt värde för fästpunktsattributet för objektet i den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-194">After it receives the object from sync engine, the connected data source creates a unique value for the anchor attribute of the object.</span></span>

<span data-ttu-id="f359f-195">Följande bild visar hur en export-objektet har skapats med hjälp av identitetsinformation i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-195">The following illustration shows how an export object is created by using identity information in the metaverse.</span></span>

![Arch4](./media/active-directory-aadconnectsync-understanding-architecture/arch4.png)

<span data-ttu-id="f359f-197">Synkroniseringsmotorn bekräftar export av objektet genom att importera objekt från den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-197">The sync engine confirms the export of the object by reimporting the object from the connected data source.</span></span> <span data-ttu-id="f359f-198">Exportera objekt blir importera objekt när Synkroniseringsmotorn tar emot dem under nästa import från den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-198">Export objects become import objects when sync engine receives them during the next import from that connected data source.</span></span>

### <a name="placeholders"></a><span data-ttu-id="f359f-199">Platshållare</span><span class="sxs-lookup"><span data-stu-id="f359f-199">Placeholders</span></span>
<span data-ttu-id="f359f-200">Synkroniseringsmotorn använder en platt namnrymd för att lagra objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-200">The sync engine uses a flat namespace to store objects.</span></span> <span data-ttu-id="f359f-201">Men vissa anslutna datakällor, till exempel Active Directory och använda ett hierarkiskt namnområde.</span><span class="sxs-lookup"><span data-stu-id="f359f-201">However, some connected data sources such as Active Directory use a hierarchical namespace.</span></span> <span data-ttu-id="f359f-202">För att omvandla information från ett hierarkiskt namnområde i en platt namnområde, använder Synkroniseringsmotorn platshållare för att behålla hierarkin.</span><span class="sxs-lookup"><span data-stu-id="f359f-202">To transform information from a hierarchical namespace into a flat namespace, sync engine uses placeholders to preserve the hierarchy.</span></span>

<span data-ttu-id="f359f-203">Varje platshållare representerar en komponent (till exempel en organisationsenhet) i en hierarkisk objektnamn som inte har importerats till Synkroniseringsmotorn men krävs för att konstruera det hierarkiska namnet.</span><span class="sxs-lookup"><span data-stu-id="f359f-203">Each placeholder represents a component (for example, an organizational unit) of an object's hierarchical name that has not been imported into sync engine but is required to construct the hierarchical name.</span></span> <span data-ttu-id="f359f-204">De fyller luckor som skapats av referenser i den anslutna datakällan till objekt som inte mellanlagring objekt i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-204">They fill gaps created by references in the connected data source to objects that are not staging objects in the connector space.</span></span>

<span data-ttu-id="f359f-205">Synkroniseringsmotorn använder också platshållare för att lagra refererade objekt som ännu inte importerats.</span><span class="sxs-lookup"><span data-stu-id="f359f-205">The sync engine also uses placeholders to store referenced objects that have not yet been imported.</span></span> <span data-ttu-id="f359f-206">Om synkronisering är konfigurerad för att inkludera attributet manager för till exempel den *Abbie Spencer* objekt och det mottagna värdet är ett objekt som inte har importerats ännu, t.ex *CN Lee Sperry, CN = = Users, DC = fabrikam, DC = com* , manager informationen lagras som platshållare i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-206">For example, if sync is configured to include the manager attribute for the *Abbie Spencer* object and the received value is an object that has not been imported yet, such as *CN=Lee Sperry,CN=Users,DC=fabrikam,DC=com*, the manager information is stored as placeholders in the connector space.</span></span> <span data-ttu-id="f359f-207">Om objektet manager senare importeras skrivs platshållarobjekt över av det mellanlagrade objekt som representerar hanteraren.</span><span class="sxs-lookup"><span data-stu-id="f359f-207">If the manager object is later imported, the placeholder object is overwritten by the staging object that represents the manager.</span></span>

### <a name="metaverse-objects"></a><span data-ttu-id="f359f-208">Metaversum-objekt</span><span class="sxs-lookup"><span data-stu-id="f359f-208">Metaverse objects</span></span>
<span data-ttu-id="f359f-209">En metaversumobjekt innehåller aggregerade vyn att Synkroniseringsmotorn har mellanlagrade objekt i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-209">A metaverse object contains the aggregated view that sync engine has of the staging objects in the connector space.</span></span> <span data-ttu-id="f359f-210">Synkroniseringsmotorn skapar metaversum-objekt med hjälp av informationen i Importera objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-210">Sync engine creates metaverse objects by using the information in import objects.</span></span> <span data-ttu-id="f359f-211">Flera connector utrymme objekt kan länkas till en enda metaversumobjekt, men ett utrymme connector-objekt kan inte länkas till fler än ett metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-211">Several connector space objects can be linked to a single metaverse object, but a connector space object cannot be linked to more than one metaverse object.</span></span>

<span data-ttu-id="f359f-212">Metaversum-objekt kan inte skapas eller tas bort manuellt.</span><span class="sxs-lookup"><span data-stu-id="f359f-212">Metaverse objects cannot be manually created or deleted.</span></span> <span data-ttu-id="f359f-213">Synkroniseringsmotorn tar automatiskt bort metaversum-objekt som inte har en länk till koppling utrymme objekt i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-213">The sync engine automatically deletes metaverse objects that do not have a link to any connector space object in the connector space.</span></span>

<span data-ttu-id="f359f-214">Om du vill mappa objekt inom en ansluten datakälla till en motsvarande objekttyp i metaversum ger Synkroniseringsmotorn ett Utökningsbart schema med en fördefinierad uppsättning objekttyper och motsvarande attribut.</span><span class="sxs-lookup"><span data-stu-id="f359f-214">To map objects within a connected data source to a corresponding object type within the metaverse, sync engine provides an extensible schema with a predefined set of object types and associated attributes.</span></span> <span data-ttu-id="f359f-215">Du kan skapa nya objekttyper och attribut för metaversum-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-215">You can create new object types and attributes for metaverse objects.</span></span> <span data-ttu-id="f359f-216">Attribut kan vara eller flera värden och attributtyperna kan vara strängar, referenser, siffror och booleska värden.</span><span class="sxs-lookup"><span data-stu-id="f359f-216">Attributes can be single-valued or multivalued, and the attribute types can be strings, references, numbers, and Boolean values.</span></span>

### <a name="relationships-between-staging-objects-and-metaverse-objects"></a><span data-ttu-id="f359f-217">Relationer mellan fristående och metaversum-objekt</span><span class="sxs-lookup"><span data-stu-id="f359f-217">Relationships between staging objects and metaverse objects</span></span>
<span data-ttu-id="f359f-218">Inom namnområdet sync motorn aktiveras dataflödet som länk relationen mellan fristående och metaversum-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-218">Within the sync engine namespace, the data flow is enabled by the link relationship between staging objects and metaverse objects.</span></span> <span data-ttu-id="f359f-219">Ett fristående objekt som är kopplad till ett metaversumobjekt kallas en **kopplade objekt** (eller **kopplingsobjektet**).</span><span class="sxs-lookup"><span data-stu-id="f359f-219">A staging object that is linked to a metaverse object is called a **joined object** (or **connector object**).</span></span> <span data-ttu-id="f359f-220">Ett fristående objekt som inte är kopplad till ett metaversumobjekt kallas en **degraderingsprocessen objektet** (eller **disconnector objektet**).</span><span class="sxs-lookup"><span data-stu-id="f359f-220">A staging object that is not linked to a metaverse object is called a **disjoined object** (or **disconnector object**).</span></span> <span data-ttu-id="f359f-221">Villkoren ansluten och degraderingsprocessen är att föredra framför inte förväxlas med kopplingar ansvarar för att importera och exportera data från en ansluten katalog.</span><span class="sxs-lookup"><span data-stu-id="f359f-221">The terms joined and disjoined are preferred to not confuse with the Connectors responsible for importing and exporting data from a connected directory.</span></span>

<span data-ttu-id="f359f-222">Platshållare är aldrig länkade till ett metaversumobjekt</span><span class="sxs-lookup"><span data-stu-id="f359f-222">Placeholders are never linked to a metaverse object</span></span>

<span data-ttu-id="f359f-223">En domänansluten objektet består av ett fristående objekt och dess länkade relation till en enda metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-223">A joined object comprises a staging object and its linked relationship to a single metaverse object.</span></span> <span data-ttu-id="f359f-224">Kopplade objekt används för att synkronisera attributvärden mellan en koppling utrymme objekt och metaversum-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-224">Joined objects are used to synchronize attribute values between a connector space object and a metaverse object.</span></span>

<span data-ttu-id="f359f-225">När ett fristående objekt blir en domänansluten objektet under synkroniseringen, kan attribut flöda mellan fristående objektet och metaversum-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-225">When a staging object becomes a joined object during synchronization, attributes can flow between the staging object and the metaverse object.</span></span> <span data-ttu-id="f359f-226">Attributflöde är dubbelriktad och konfigureras med hjälp av attributet importregler och exportera attributregler.</span><span class="sxs-lookup"><span data-stu-id="f359f-226">Attribute flow is bidirectional and is configured by using import attribute rules and export attribute rules.</span></span>

<span data-ttu-id="f359f-227">En enskild koppling utrymme objekt kan länkas till endast en metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-227">A single connector space object can be linked to only one metaverse object.</span></span> <span data-ttu-id="f359f-228">Varje metaversumobjekt kan dock länkas till flera connector utrymme objekt i samma eller olika kopplingens utrymmen som visas i följande bild.</span><span class="sxs-lookup"><span data-stu-id="f359f-228">However, each metaverse object can be linked to multiple connector space objects in the same or in different connector spaces, as shown in the following illustration.</span></span>

![Arch5](./media/active-directory-aadconnectsync-understanding-architecture/arch5.png)

<span data-ttu-id="f359f-230">Länkade relationen mellan mellanlagrade objekt och ett metaversumobjekt är permanent och kan tas bort bara av regler som du anger.</span><span class="sxs-lookup"><span data-stu-id="f359f-230">The linked relationship between the staging object and a metaverse object is persistent and can be removed only by rules that you specify.</span></span>

<span data-ttu-id="f359f-231">En frånkopplad objektet är ett fristående objekt som inte är länkad till någon metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-231">A disjoined object is a staging object that is not linked to any metaverse object.</span></span> <span data-ttu-id="f359f-232">Attributet värden i en frånkopplad objektet inte är bearbeta några ytterligare i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-232">The attribute values of a disjoined object are not processed any further within the metaverse.</span></span> <span data-ttu-id="f359f-233">Attributvärden för motsvarande objekt i den anslutna datakällan uppdateras inte av Synkroniseringsmotorn.</span><span class="sxs-lookup"><span data-stu-id="f359f-233">The attribute values of the corresponding object in the connected data source are not updated by sync engine.</span></span>

<span data-ttu-id="f359f-234">Du kan lagra identitetsinformation i Synkroniseringsmotorn och bearbetar den senare genom att använda skilda objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-234">By using disjoined objects, you can store identity information in sync engine and process it later.</span></span> <span data-ttu-id="f359f-235">Som ett fristående objekt som en frånkopplad objekt i anslutningsplatsen har många fördelar.</span><span class="sxs-lookup"><span data-stu-id="f359f-235">Keeping a staging object as a disjoined object in the connector space has many advantages.</span></span> <span data-ttu-id="f359f-236">Eftersom systemet har redan mellanlagrat nödvändig information om det här objektet, behöver du inte skapa en representation av det här objektet igen under nästa importen från den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-236">Because the system has already staged the required information about this object, it is not necessary to create a representation of this object again during the next import from the connected data source.</span></span> <span data-ttu-id="f359f-237">På så sätt kan Synkroniseringsmotorn har alltid en fullständig ögonblicksbild av den anslutna datakällan, även om det inte finns någon anslutning till den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-237">This way, sync engine always has a complete snapshot of the connected data source, even if there is no current connection to the connected data source.</span></span> <span data-ttu-id="f359f-238">Frånkopplad objekt kan konverteras till domänanslutna objekt och omvänt, beroende på de regler som du anger.</span><span class="sxs-lookup"><span data-stu-id="f359f-238">Disjoined objects can be converted into joined objects, and vice versa, depending on the rules that you specify.</span></span>

<span data-ttu-id="f359f-239">Ett importobjekt skapas som en frånkopplad objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-239">An import object is created as a disjoined object.</span></span> <span data-ttu-id="f359f-240">En export-objektet måste vara en domänansluten objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-240">An export object must be a joined object.</span></span> <span data-ttu-id="f359f-241">Systemlogik tillämpar regeln och tar bort alla export-objekt som inte är domänansluten objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-241">The system logic enforces this rule and deletes every export object that is not a joined object.</span></span>

## <a name="sync-engine-identity-management-process"></a><span data-ttu-id="f359f-242">Motorn identity management-synkroniseringen</span><span class="sxs-lookup"><span data-stu-id="f359f-242">Sync engine identity management process</span></span>
<span data-ttu-id="f359f-243">Process för hantering av identiteter styr hur identitetsinformation uppdateras mellan olika anslutna datakällor.</span><span class="sxs-lookup"><span data-stu-id="f359f-243">The identity management process controls how identity information is updated between different connected data sources.</span></span> <span data-ttu-id="f359f-244">Identitetshantering sker i tre processer:</span><span class="sxs-lookup"><span data-stu-id="f359f-244">Identity management occurs in three processes:</span></span>

* <span data-ttu-id="f359f-245">Importera</span><span class="sxs-lookup"><span data-stu-id="f359f-245">Import</span></span>
* <span data-ttu-id="f359f-246">Synkronisering</span><span class="sxs-lookup"><span data-stu-id="f359f-246">Synchronization</span></span>
* <span data-ttu-id="f359f-247">Exportera</span><span class="sxs-lookup"><span data-stu-id="f359f-247">Export</span></span>

<span data-ttu-id="f359f-248">Under importen utvärderar Synkroniseringsmotorn inkommande identitetsinformation från en ansluten datakälla.</span><span class="sxs-lookup"><span data-stu-id="f359f-248">During the import process, sync engine evaluates the incoming identity information from a connected data source.</span></span> <span data-ttu-id="f359f-249">När ändringar identifieras den antingen skapar nya mellanlagrade objekt eller uppdaterar befintliga mellanlagrade objekt i anslutningsplatsen för synkronisering.</span><span class="sxs-lookup"><span data-stu-id="f359f-249">When changes are detected, it either creates new staging objects or updates existing staging objects in the connector space for synchronization.</span></span>

<span data-ttu-id="f359f-250">Under synkroniseringen Synkroniseringsmotorn uppdateras för att återspegla ändringarna som skett i anslutningsplatsen metaversum samt anslutningsplatsen för att återspegla ändringarna som skett i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-250">During the synchronization process, sync engine updates the metaverse to reflect changes that have occurred in the connector space and updates the connector space to reflect changes that have occurred in the metaverse.</span></span>

<span data-ttu-id="f359f-251">Under exporten skickar Synkroniseringsmotorn ändringar som mellanlagras på mellanlagring objekt och som är flaggade som väntande export.</span><span class="sxs-lookup"><span data-stu-id="f359f-251">During the export process, sync engine pushes out changes that are staged on staging objects and that are flagged as pending export.</span></span>

<span data-ttu-id="f359f-252">I följande illustration visas där alla processer som används som identitet information flödar från en ansluten datakälla till en annan.</span><span class="sxs-lookup"><span data-stu-id="f359f-252">The following illustration shows where each of the processes occurs as identity information flows from one connected data source to another.</span></span>

![Arch6](./media/active-directory-aadconnectsync-understanding-architecture/arch6.png)

### <a name="import-process"></a><span data-ttu-id="f359f-254">Importen</span><span class="sxs-lookup"><span data-stu-id="f359f-254">Import process</span></span>
<span data-ttu-id="f359f-255">Under importen utvärderar Synkroniseringsmotorn uppdateringar till identitetsinformation.</span><span class="sxs-lookup"><span data-stu-id="f359f-255">During the import process, sync engine evaluates updates to identity information.</span></span> <span data-ttu-id="f359f-256">Synkroniseringsmotorn jämför identitetsinformation togs emot från den anslutna datakällan med ID-information om ett fristående objekt och anger om uppdateringar krävs för fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-256">Sync engine compares the identity information received from the connected data source with the identity information about a staging object and determines whether the staging object requires updates.</span></span> <span data-ttu-id="f359f-257">Om det är nödvändigt att uppdatera objektet mellanlagring med nya data flaggas mellanlagrade objekt som väntande import.</span><span class="sxs-lookup"><span data-stu-id="f359f-257">If it is necessary to update the staging object with new data, the staging object is flagged as pending import.</span></span>

<span data-ttu-id="f359f-258">Synkroniseringsmotorn kan bearbeta den identitetsinformation som har ändrats av mellanlagring objekt i anslutningsplatsen innan synkroniseringen.</span><span class="sxs-lookup"><span data-stu-id="f359f-258">By staging objects in the connector space before synchronization, sync engine can process only the identity information that has changed.</span></span> <span data-ttu-id="f359f-259">Den här processen ger följande fördelar:</span><span class="sxs-lookup"><span data-stu-id="f359f-259">This process provides the following benefits:</span></span>

* <span data-ttu-id="f359f-260">**Effektiv synkronisering**.</span><span class="sxs-lookup"><span data-stu-id="f359f-260">**Efficient synchronization**.</span></span> <span data-ttu-id="f359f-261">Mängden data som bearbetats under synkronisering minimeras.</span><span class="sxs-lookup"><span data-stu-id="f359f-261">The amount of data processed during synchronization is minimized.</span></span>
* <span data-ttu-id="f359f-262">**Effektiv omsynkroniseringen**.</span><span class="sxs-lookup"><span data-stu-id="f359f-262">**Efficient resynchronization**.</span></span> <span data-ttu-id="f359f-263">Du kan ändra hur Synkroniseringsmotorn bearbetar identitetsinformation utan återansluta Synkroniseringsmotorn till datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-263">You can change how sync engine processes identity information without reconnecting the sync engine to the data source.</span></span>
* <span data-ttu-id="f359f-264">**Möjlighet att förhandsgranska synkronisering**.</span><span class="sxs-lookup"><span data-stu-id="f359f-264">**Opportunity to preview synchronization**.</span></span> <span data-ttu-id="f359f-265">Du kan förhandsgranska synkronisering för att kontrollera att din antaganden om hanteringen identitet är korrekta.</span><span class="sxs-lookup"><span data-stu-id="f359f-265">You can preview synchronization to verify that your assumptions about the identity management process are correct.</span></span>

<span data-ttu-id="f359f-266">För varje objekt som angetts för kopplingen försöker Synkroniseringsmotorn hitta en representation av objektet i anslutningsplatsen av anslutningen.</span><span class="sxs-lookup"><span data-stu-id="f359f-266">For each object specified in the Connector, the sync engine first tries to locate a representation of the object in the connector space of the Connector.</span></span> <span data-ttu-id="f359f-267">Synkroniseringsmotorn undersöker alla mellanlagrade objekt i anslutningsplatsen och försöker hitta ett motsvarande mellanlagrade objekt som har en matchande fästpunktsattributet.</span><span class="sxs-lookup"><span data-stu-id="f359f-267">Sync engine examines all staging objects in the connector space and tries to find a corresponding staging object that has a matching anchor attribute.</span></span> <span data-ttu-id="f359f-268">Om inga befintliga mellanlagrade objekt har en matchande fästpunktsattributet, försöker Synkroniseringsmotorn hitta ett motsvarande mellanlagrade objekt med samma unika namn.</span><span class="sxs-lookup"><span data-stu-id="f359f-268">If no existing staging object has a matching anchor attribute, sync engine tries to find a corresponding staging object with the same distinguished name.</span></span>

<span data-ttu-id="f359f-269">När Synkroniseringsmotorn hittar ett fristående objekt som matchar med unika namn, men inte genom fästpunkt, händer särskilt följande:</span><span class="sxs-lookup"><span data-stu-id="f359f-269">When sync engine finds a staging object that matches by distinguished name but not by anchor, the following special behavior occurs:</span></span>

* <span data-ttu-id="f359f-270">Om objektet finns i anslutningsplatsen har inga fästpunkt sedan Synkroniseringsmotorn tar bort objektet från anslutningsplatsen och markerar metaversum-objekt som är länkad till som **försök etablering på nästa synkronisering kör**.</span><span class="sxs-lookup"><span data-stu-id="f359f-270">If the object located in the connector space has no anchor, then sync engine removes this object from the connector space and marks the metaverse object it is linked to as **retry provisioning on next synchronization run**.</span></span> <span data-ttu-id="f359f-271">Sedan skapar den nya importera objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-271">Then it creates the new import object.</span></span>
* <span data-ttu-id="f359f-272">Om objektet finns i anslutningsplatsen har ankare, förutsätter Synkroniseringsmotorn att det här objektet har antingen ändrats eller tagits bort i ansluten katalog.</span><span class="sxs-lookup"><span data-stu-id="f359f-272">If the object located in the connector space has an anchor, then sync engine assumes that this object has either been renamed or deleted in the connected directory.</span></span> <span data-ttu-id="f359f-273">Tilldelar ett tillfälligt, nya unikt namn för objektet connector utrymme så att den kan mellanlagra inkommande objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-273">It assigns a temporary, new distinguished name for the connector space object so that it can stage the incoming object.</span></span> <span data-ttu-id="f359f-274">Det gamla objektet blir **tillfälligt**, väntar på att kopplingen för att importera Byt namn på eller ta bort för att lösa problemet.</span><span class="sxs-lookup"><span data-stu-id="f359f-274">The old object then becomes **transient**, waiting for the Connector to import the rename or deletion to resolve the situation.</span></span>

<span data-ttu-id="f359f-275">Om Synkroniseringsmotorn hittar ett fristående objekt som motsvarar det angivna objektet i kopplingen, anger vilken typ av ändringarna ska tillämpas.</span><span class="sxs-lookup"><span data-stu-id="f359f-275">If sync engine locates a staging object that corresponds to the object specified in the Connector, it determines what kind of changes to apply.</span></span> <span data-ttu-id="f359f-276">Till exempel Synkroniseringsmotorn kan byta namn på eller ta bort objekt i den anslutna datakällan eller det kan bara uppdatera objektets attributvärden.</span><span class="sxs-lookup"><span data-stu-id="f359f-276">For example, sync engine might rename or delete the object in the connected data source, or it might only update the object’s attribute values.</span></span>

<span data-ttu-id="f359f-277">Mellanlagring objekt med uppdaterade data är markerade som väntande import.</span><span class="sxs-lookup"><span data-stu-id="f359f-277">Staging objects with updated data are marked as pending import.</span></span> <span data-ttu-id="f359f-278">Det finns olika typer av väntande import.</span><span class="sxs-lookup"><span data-stu-id="f359f-278">Different types of pending imports are available.</span></span> <span data-ttu-id="f359f-279">Beroende på resultatet av importen har ett fristående objekt i anslutningsplatsen något av följande väntande import typer:</span><span class="sxs-lookup"><span data-stu-id="f359f-279">Depending on the result of the import process, a staging object in the connector space has one of the following pending import types:</span></span>

* <span data-ttu-id="f359f-280">**Ingen**.</span><span class="sxs-lookup"><span data-stu-id="f359f-280">**None**.</span></span> <span data-ttu-id="f359f-281">Det finns inga ändringar i alla attribut för fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-281">No changes to any of the attributes of the staging object are available.</span></span> <span data-ttu-id="f359f-282">Synkroniseringsmotorn flaggas inte den här typen som väntande import.</span><span class="sxs-lookup"><span data-stu-id="f359f-282">Sync engine does not flag this type as pending import.</span></span>
* <span data-ttu-id="f359f-283">**Lägg till**.</span><span class="sxs-lookup"><span data-stu-id="f359f-283">**Add**.</span></span> <span data-ttu-id="f359f-284">Fristående objektet är ett nytt importera objekt i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-284">The staging object is a new import object in the connector space.</span></span> <span data-ttu-id="f359f-285">Synkroniseringsmotorn flaggor den här typen som väntande import för ytterligare bearbetning i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-285">Sync engine flags this type as pending import for additional processing in the metaverse.</span></span>
* <span data-ttu-id="f359f-286">**Uppdatera**.</span><span class="sxs-lookup"><span data-stu-id="f359f-286">**Update**.</span></span> <span data-ttu-id="f359f-287">Synkroniseringsmotorn söker efter ett motsvarande mellanlagrade objekt i anslutningsplatsen och flaggor den här typen som väntande import så att uppdateringar till attributen kan bearbetas i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-287">Sync engine finds a corresponding staging object in the connector space and flags this type as pending import so that updates to the attributes can be processed in the metaverse.</span></span> <span data-ttu-id="f359f-288">Byta namn på objekt ingår.</span><span class="sxs-lookup"><span data-stu-id="f359f-288">Updates include object renaming.</span></span>
* <span data-ttu-id="f359f-289">**Ta bort**.</span><span class="sxs-lookup"><span data-stu-id="f359f-289">**Delete**.</span></span> <span data-ttu-id="f359f-290">Synkroniseringsmotorn söker efter ett motsvarande mellanlagrade objekt i anslutningsplatsen och flaggor den här typen som väntande import så att det kopplade objektet kan tas bort.</span><span class="sxs-lookup"><span data-stu-id="f359f-290">Sync engine finds a corresponding staging object in the connector space and flags this type as pending import so that the joined object can be deleted.</span></span>
* <span data-ttu-id="f359f-291">**Ta bort/Lägg till**.</span><span class="sxs-lookup"><span data-stu-id="f359f-291">**Delete/Add**.</span></span> <span data-ttu-id="f359f-292">Synkroniseringsmotorn söker efter ett motsvarande mellanlagrade objekt i anslutningsplatsen, men matchar inte typerna av objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-292">Sync engine finds a corresponding staging object in the connector space, but the object types do not match.</span></span> <span data-ttu-id="f359f-293">I det här fallet en delete-Lägg till ändring mellanlagras.</span><span class="sxs-lookup"><span data-stu-id="f359f-293">In this case, a delete-add modification is staged.</span></span> <span data-ttu-id="f359f-294">En delete-Lägg till ändring anger att Synkroniseringsmotorn att en fullständig synkronisering av det här objektet måste inträffa eftersom olika uppsättningar med regler som gäller för det här objektet när du objekttyp ändringar.</span><span class="sxs-lookup"><span data-stu-id="f359f-294">A delete-add modification indicates to the sync engine that a complete resynchronization of this object must occur because different sets of rules apply to this object when the object type changes.</span></span>

<span data-ttu-id="f359f-295">Det är möjligt att avsevärt minska mängden data som bearbetas under synkroniseringen eftersom gör det blir systemet för att bearbeta endast de objekt som har uppdaterat data genom att ange importstatus väntande av ett fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-295">By setting the pending import status of a staging object, it is possible to reduce significantly the amount of data processed during synchronization because doing so allows the system to process only those objects that have updated data.</span></span>

### <a name="synchronization-process"></a><span data-ttu-id="f359f-296">Processen för synkronisering</span><span class="sxs-lookup"><span data-stu-id="f359f-296">Synchronization process</span></span>
<span data-ttu-id="f359f-297">Synkronisering består av två relaterade processer:</span><span class="sxs-lookup"><span data-stu-id="f359f-297">Synchronization consists of two related processes:</span></span>

* <span data-ttu-id="f359f-298">Inkommande synkronisering när innehållet i metaversum uppdateras med hjälp av data i anslutningsplatsen.</span><span class="sxs-lookup"><span data-stu-id="f359f-298">Inbound synchronization, when the content of the metaverse is updated by using the data in the connector space.</span></span>
* <span data-ttu-id="f359f-299">Utgående synkronisering när innehållet i anslutningsplatsen uppdateras med hjälp av data i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-299">Outbound synchronization, when the content of the connector space is updated by using data in the metaverse.</span></span>

<span data-ttu-id="f359f-300">Med hjälp av informationen som mellanlagras i anslutningsplatsen skapas den inkommande synkronisering i metaversum integrerad vy över de data som lagras i de anslutna datakällorna.</span><span class="sxs-lookup"><span data-stu-id="f359f-300">By using the information staged in the connector space, the inbound synchronization process creates in the metaverse the integrated view of the data that is stored in the connected data sources.</span></span> <span data-ttu-id="f359f-301">Alla mellanlagrade objekt eller endast de som en väntande importera information aggregeras, beroende på hur reglerna har konfigurerats.</span><span class="sxs-lookup"><span data-stu-id="f359f-301">Either all staging objects or only those with a pending import information are aggregated, depending on how the rules are configured.</span></span>

<span data-ttu-id="f359f-302">Utgående synkronisering processen uppdateringar exportera objekt när metaversum objekt ändras.</span><span class="sxs-lookup"><span data-stu-id="f359f-302">The outbound synchronization process updates export objects when metaverse objects change.</span></span>

<span data-ttu-id="f359f-303">Inkommande synkronisering skapar vyn integrerad i metaversum identitet information som tas emot från anslutna datakällor.</span><span class="sxs-lookup"><span data-stu-id="f359f-303">Inbound synchronization creates the integrated view in the metaverse of the identity information that is received from the connected data sources.</span></span> <span data-ttu-id="f359f-304">Synkroniseringsmotorn kan bearbeta identitetsinformation när som helst genom att använda den senaste informationen för identitet som den har från den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-304">Sync engine can process identity information at any time by using the latest identity information that it has from the connected data source.</span></span>

<span data-ttu-id="f359f-305">**Inkommande synkronisering**</span><span class="sxs-lookup"><span data-stu-id="f359f-305">**Inbound synchronization**</span></span>

<span data-ttu-id="f359f-306">Inkommande synkronisering innehåller följande processer:</span><span class="sxs-lookup"><span data-stu-id="f359f-306">Inbound synchronization includes the following processes:</span></span>

* <span data-ttu-id="f359f-307">**Etablera** (kallas även **projektion** om det är viktigt att skilja den här processen från utgående synkronisering etablering).</span><span class="sxs-lookup"><span data-stu-id="f359f-307">**Provision** (also called **Projection** if it is important to distinguish this process from outbound synchronization provisioning).</span></span> <span data-ttu-id="f359f-308">Synkroniseringsmotorn länkar dem skapas ett nytt metaversumobjekt baserat på ett fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-308">The Sync engine creates a new metaverse object based on a staging object and links them.</span></span> <span data-ttu-id="f359f-309">Etablera är en åtgärd på objektnivå.</span><span class="sxs-lookup"><span data-stu-id="f359f-309">Provision is an object-level operation.</span></span>
* <span data-ttu-id="f359f-310">**Ansluta till**.</span><span class="sxs-lookup"><span data-stu-id="f359f-310">**Join**.</span></span> <span data-ttu-id="f359f-311">Synkroniseringsmotorn länkar ett fristående objekt till ett befintligt metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-311">The Sync engine links a staging object to an existing metaverse object.</span></span> <span data-ttu-id="f359f-312">En koppling är en åtgärd på objektnivå.</span><span class="sxs-lookup"><span data-stu-id="f359f-312">A join is an object-level operation.</span></span>
* <span data-ttu-id="f359f-313">**Importera attributflöde**.</span><span class="sxs-lookup"><span data-stu-id="f359f-313">**Import attribute flow**.</span></span> <span data-ttu-id="f359f-314">Synkroniseringsmotorn uppdaterar attributvärden och kallas attributflöde för objektet i metaversum.</span><span class="sxs-lookup"><span data-stu-id="f359f-314">Sync engine updates the attribute values, called attribute flow, of the object in the metaverse.</span></span> <span data-ttu-id="f359f-315">Importera attributflöde är ett attribut på objektnivå åtgärd som kräver en länk mellan ett fristående objekt och metaversum-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-315">Import attribute flow is an attribute-level operation that requires a link between a staging object and a metaverse object.</span></span>

<span data-ttu-id="f359f-316">Etablera är den enda process som skapar objekt i metaversumet.</span><span class="sxs-lookup"><span data-stu-id="f359f-316">Provision is the only process that creates objects in the metaverse.</span></span> <span data-ttu-id="f359f-317">Etablera påverkar endast importera objekt som är skilda objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-317">Provision affects only import objects that are disjoined objects.</span></span> <span data-ttu-id="f359f-318">Under etablera skapar Synkroniseringsmotorn ett metaversumobjekt som motsvarar objekttypen för objektet import och upprättar en länk mellan både objekt, vilket skapar ett objekt som är kopplade.</span><span class="sxs-lookup"><span data-stu-id="f359f-318">During provision, sync engine creates a metaverse object that corresponds to the object type of the import object and establishes a link between both objects, thus creating a joined object.</span></span>

<span data-ttu-id="f359f-319">Join-processen upprättar även en länk mellan importera objekt och metaversum-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-319">The join process also establishes a link between import objects and a metaverse object.</span></span> <span data-ttu-id="f359f-320">Skillnaden mellan koppling och etablera är att ansluta till processen kräver att importera objekt är kopplade till en befintlig metaversumobjekt där etablera process skapar ett nytt metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-320">The difference between join and provision is that the join process requires that the import object are linked to an existing metaverse object, where the provision process creates a new metaverse object.</span></span>

<span data-ttu-id="f359f-321">Synkroniseringsmotorn försöker ansluta ett importera objekt till ett metaversumobjekt med hjälp av villkor som angetts i konfigurationen för Synkroniseringsregeln.</span><span class="sxs-lookup"><span data-stu-id="f359f-321">Sync engine tries to join an import object to a metaverse object by using criteria that is specified in the Synchronization Rule configuration.</span></span>

<span data-ttu-id="f359f-322">Under etablera och koppling länkar Synkroniseringsmotorn skilda objekt till ett metaversumobjekt gör dem ansluten.</span><span class="sxs-lookup"><span data-stu-id="f359f-322">During the provision and join processes, sync engine links a disjoined object to a metaverse object, making them joined.</span></span> <span data-ttu-id="f359f-323">När dessa åtgärder på objektnivå har slutförts, kan Synkroniseringsmotorn uppdatera attributvärden för den associerade metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-323">After these object-level operations are completed, sync engine can update the attribute values of the associated metaverse object.</span></span> <span data-ttu-id="f359f-324">Den här processen kallas attributflöde för import.</span><span class="sxs-lookup"><span data-stu-id="f359f-324">This process is called import attribute flow.</span></span>

<span data-ttu-id="f359f-325">Importera attributflöde uppstår på alla importera objekt som innehåller nya data och kopplas till ett metaversumobjekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-325">Import attribute flow occurs on all import objects that carry new data and are linked to a metaverse object.</span></span>

<span data-ttu-id="f359f-326">**Utgående synkronisering**</span><span class="sxs-lookup"><span data-stu-id="f359f-326">**Outbound synchronization**</span></span>

<span data-ttu-id="f359f-327">Utgående synkronisering uppdateringar exportera objekt när en metaversumobjekt ändra men tas inte bort.</span><span class="sxs-lookup"><span data-stu-id="f359f-327">Outbound synchronization updates export objects when a metaverse object change but is not deleted.</span></span> <span data-ttu-id="f359f-328">Syftet med utgående synkronisering är att utvärdera om ändringar i metaversum objekt kräver uppdateringar för fristående objekt i kopplingens utrymmen.</span><span class="sxs-lookup"><span data-stu-id="f359f-328">The objective of outbound synchronization is to evaluate whether changes to metaverse objects require updates to staging objects in the connector spaces.</span></span> <span data-ttu-id="f359f-329">I vissa fall kan ändringarna kan kräva att mellanlagring objekt i alla kopplingens utrymmen uppdateras.</span><span class="sxs-lookup"><span data-stu-id="f359f-329">In some cases, the changes can require that staging objects in all connector spaces be updated.</span></span> <span data-ttu-id="f359f-330">Mellanlagring objekt som har ändrats flaggas som väntande export, gör dem exportera objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-330">Staging objects that are changed are flagged as pending export, making them export objects.</span></span> <span data-ttu-id="f359f-331">Dessa exportalternativ objekt senare push-installeras på den anslutna datakällan under exporten.</span><span class="sxs-lookup"><span data-stu-id="f359f-331">These export objects are later pushed out to the connected data source during the export process.</span></span>

<span data-ttu-id="f359f-332">Utgående synkronisering har tre processer:</span><span class="sxs-lookup"><span data-stu-id="f359f-332">Outbound synchronization has three processes:</span></span>

* <span data-ttu-id="f359f-333">**Etablering**</span><span class="sxs-lookup"><span data-stu-id="f359f-333">**Provisioning**</span></span>
* <span data-ttu-id="f359f-334">**Avetablering**</span><span class="sxs-lookup"><span data-stu-id="f359f-334">**Deprovisioning**</span></span>
* <span data-ttu-id="f359f-335">**Exportera attributflöde**</span><span class="sxs-lookup"><span data-stu-id="f359f-335">**Export attribute flow**</span></span>

<span data-ttu-id="f359f-336">Etablering och borttagning finns både på objektnivå-operationer.</span><span class="sxs-lookup"><span data-stu-id="f359f-336">Provisioning and deprovisioning are both object-level operations.</span></span> <span data-ttu-id="f359f-337">Avetablering beror på etablering eftersom endast etablering kan starta den.</span><span class="sxs-lookup"><span data-stu-id="f359f-337">Deprovisioning depends on provisioning because only provisioning can initiate it.</span></span> <span data-ttu-id="f359f-338">Avetablering utlöses när etablering tar bort länken mellan ett metaversumobjekt och en export-objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-338">Deprovisioning is triggered when provisioning removes the link between a metaverse object and an export object.</span></span>

<span data-ttu-id="f359f-339">Etablering utlöses alltid när ändringar tillämpas på objekt i metaversumet.</span><span class="sxs-lookup"><span data-stu-id="f359f-339">Provisioning is always triggered when changes are applied to objects in the metaverse.</span></span> <span data-ttu-id="f359f-340">När ändringar görs metaversum-objekt, utför Synkroniseringsmotorn någon av följande uppgifter som en del av etableringsprocessen:</span><span class="sxs-lookup"><span data-stu-id="f359f-340">When changes are made to metaverse objects, sync engine can perform any of the following tasks as part of the provisioning process:</span></span>

* <span data-ttu-id="f359f-341">Skapa kopplade objekt, där en metaversumobjekt är länkad till en nyligen skapade export-objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-341">Create joined objects, where a metaverse object is linked to a newly created export object.</span></span>
* <span data-ttu-id="f359f-342">Byt namn på en domänansluten objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-342">Rename a joined object.</span></span>
* <span data-ttu-id="f359f-343">Sedan en frånkoppling av länkar mellan en metaversumobjekt och organisering objekt, skapa en frånkopplad objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-343">Disjoin links between a metaverse object and staging objects, creating a disjoined object.</span></span>

<span data-ttu-id="f359f-344">Om etablering kräver Synkroniseringsmotorn för att skapa ett nytt connector-objekt, är fristående objektet som är länkad metaversum-objekt alltid ett export-objekt eftersom objektet inte finns ännu i den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-344">If provisioning requires sync engine to create a new connector object, the staging object to which the metaverse object is linked is always an export object, because the object does not yet exist in the connected data source.</span></span>

<span data-ttu-id="f359f-345">Om etablering kräver Synkroniseringsmotorn till sedan en frånkoppling av ett kopplade objekt, skapar en frånkopplad objekt utlöses avetablering.</span><span class="sxs-lookup"><span data-stu-id="f359f-345">If provisioning requires sync engine to disjoin a joined object, creating a disjoined object, deprovisioning is triggered.</span></span> <span data-ttu-id="f359f-346">Avetableringsprocessen tar bort objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-346">The deprovisioning process deletes the object.</span></span>

<span data-ttu-id="f359f-347">Under avetablering bort tar bort en export-objektet inte fysiskt objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-347">During deprovisioning, deleting an export object does not physically delete the object.</span></span> <span data-ttu-id="f359f-348">Objektet har flaggats som **bort**, vilket innebär att borttagningen mellanlagras på objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-348">The object is flagged as **deleted**, which means that the delete operation is staged on the object.</span></span>

<span data-ttu-id="f359f-349">Exportera attributflöde inträffar också under utgående synkroniseringen, liknande sätt att importera attributflöde uppstår under inkommande synkronisering.</span><span class="sxs-lookup"><span data-stu-id="f359f-349">Export attribute flow also occurs during the outbound synchronization process, similar to the way that import attribute flow occurs during inbound synchronization.</span></span> <span data-ttu-id="f359f-350">Exportera attributflöde sker bara mellan metaversum och exportera objekt som är anslutna.</span><span class="sxs-lookup"><span data-stu-id="f359f-350">Export attribute flow occurs only between metaverse and export objects that are joined.</span></span>

### <a name="export-process"></a><span data-ttu-id="f359f-351">Exportprocess</span><span class="sxs-lookup"><span data-stu-id="f359f-351">Export process</span></span>
<span data-ttu-id="f359f-352">Under exportprocessen undersöker Synkroniseringsmotorn alla export-objekt som är flaggade som väntande export i anslutningsplatsen och skickar sedan uppdateringarna till den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-352">During the export process, sync engine examines all export objects that are flagged as pending export in the connector space, and then sends updates to the connected data source.</span></span>

<span data-ttu-id="f359f-353">Synkroniseringsmotorn kan avgöra hur framgångsrik en export men tillräckligt bestämma inte att identity management-processen har slutförts.</span><span class="sxs-lookup"><span data-stu-id="f359f-353">The sync engine can determine the success of an export but it cannot sufficiently determine that the identity management process is complete.</span></span> <span data-ttu-id="f359f-354">Objekt i den anslutna datakällan kan alltid ändras av andra processer.</span><span class="sxs-lookup"><span data-stu-id="f359f-354">Objects in the connected data source can always be changed by other processes.</span></span> <span data-ttu-id="f359f-355">Eftersom Synkroniseringsmotorn inte har en beständig anslutning till den anslutna datakällan, räcker det inte att göra antaganden om egenskaperna för ett objekt i den anslutna datakällan endast baseras på ett meddelande om lyckade exporten.</span><span class="sxs-lookup"><span data-stu-id="f359f-355">Because sync engine does not have a persistent connection to the connected data source, it is not sufficient to make assumptions about the properties of an object in the connected data source based only on a successful export notification.</span></span>

<span data-ttu-id="f359f-356">En process i den anslutna datakällan kan till exempel ändra objektets attribut tillbaka till sina ursprungliga värden (det vill säga den anslutna datakällan kan du skriva över värdena omedelbart efter att data skickas av Synkroniseringsmotorn och tillämpats i den anslutna datakällan).</span><span class="sxs-lookup"><span data-stu-id="f359f-356">For example, a process in the connected data source could change the object’s attributes back to their original values (that is, the connected data source could overwrite the values immediately after the data is pushed out by sync engine and successfully applied in the connected data source).</span></span>

<span data-ttu-id="f359f-357">Synkronisera motorn lagrar exportera och importera statusinformation om varje fristående objekt.</span><span class="sxs-lookup"><span data-stu-id="f359f-357">The sync engine stores export and import status information about each staging object.</span></span> <span data-ttu-id="f359f-358">Om du värdena för attribut som anges i attributet inkluderingslistan har ändrats sedan den senaste exporten, lagring av importera och exportera status aktiverar Synkroniseringsmotorn för att reagera på lämpligt sätt.</span><span class="sxs-lookup"><span data-stu-id="f359f-358">If values of the attributes that are specified in the attribute inclusion list have changed since the last export, the storage of import and export status enables sync engine to react appropriately.</span></span> <span data-ttu-id="f359f-359">Synkroniseringsmotorn använder importen för att bekräfta attributvärden som har exporterats till den anslutna datakällan.</span><span class="sxs-lookup"><span data-stu-id="f359f-359">Sync engine uses the import process to confirm attribute values that have been exported to the connected data source.</span></span> <span data-ttu-id="f359f-360">En jämförelse mellan den importerade och exporterade informationen kan som visas i följande illustration Synkroniseringsmotorn att avgöra om exporten har slutförts eller om den behöver upprepas.</span><span class="sxs-lookup"><span data-stu-id="f359f-360">A comparison between the imported and exported information, as shown in the following illustration, enables sync engine to determine whether the export was successful or if it needs to be repeated.</span></span>

![Arch7](./media/active-directory-aadconnectsync-understanding-architecture/arch7.png)

<span data-ttu-id="f359f-362">Till exempel om Synkroniseringsmotorn exporterar attributet C, som har värdet 5, till en ansluten datakälla, lagrar C = 5 i minnet export status.</span><span class="sxs-lookup"><span data-stu-id="f359f-362">For example, if sync engine exports attribute C, which has a value of 5, to a connected data source, it stores C=5 in its export status memory.</span></span> <span data-ttu-id="f359f-363">Varje ytterligare export på det här objektet resulterar i ett försök att exportera C = 5 till den anslutna datakällan igen eftersom Synkroniseringsmotorn förutsätter att det här värdet inte har beständigt tillämpats till objekt (det vill säga om ett annat värde har importerats nyligen från den ansluten datakälla).</span><span class="sxs-lookup"><span data-stu-id="f359f-363">Each additional export on this object results in an attempt to export C=5 to the connected data source again because sync engine assumes that this value has not been persistently applied to the object (that is, unless a different value was imported recently from the connected data source).</span></span> <span data-ttu-id="f359f-364">Export-minne rensas när C = 5 tas emot vid en import på objektet.</span><span class="sxs-lookup"><span data-stu-id="f359f-364">The export memory is cleared when C=5 is received during an import operation on the object.</span></span>

## <a name="next-steps"></a><span data-ttu-id="f359f-365">Nästa steg</span><span class="sxs-lookup"><span data-stu-id="f359f-365">Next steps</span></span>
<span data-ttu-id="f359f-366">Lär dig mer om den [Azure AD Connect-synkronisering](active-directory-aadconnectsync-whatis.md) konfiguration.</span><span class="sxs-lookup"><span data-stu-id="f359f-366">Learn more about the [Azure AD Connect sync](active-directory-aadconnectsync-whatis.md) configuration.</span></span>

<span data-ttu-id="f359f-367">Läs mer om hur du [integrerar dina lokala identiteter med Azure Active Directory](active-directory-aadconnect.md).</span><span class="sxs-lookup"><span data-stu-id="f359f-367">Learn more about [Integrating your on-premises identities with Azure Active Directory](active-directory-aadconnect.md).</span></span>

